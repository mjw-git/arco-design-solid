import { JSX } from 'solid-js';
import BaseIcon from '../BaseIcon';
export function ExpandHorizontal(props: JSX.IntrinsicElements['span']) {
  return <BaseIcon svgIcon="" {...props} />;
}

ExpandHorizontal.displayName = 'ExpandHorizontal';
