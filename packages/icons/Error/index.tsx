import { JSX } from 'solid-js';
import BaseIcon from '../BaseIcon';
export function ErrorIcon(props: JSX.IntrinsicElements['span']) {
  return <BaseIcon svgIcon="" {...props} />;
}

ErrorIcon.displayName = 'ErrorIcon';
