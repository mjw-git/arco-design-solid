import { JSX } from 'solid-js';
import BaseIcon from '../BaseIcon';
export function SearchIcon(props: JSX.IntrinsicElements['span']) {
  return <BaseIcon svgIcon="1" {...props} />;
}

SearchIcon.displayName = 'SearchIcon';
