import { LoadingIcon } from 'arco-solid-icon';
import { Empty } from 'arco-design-solid';

const Icon = () => {
  return <Empty image={<LoadingIcon />} description={'暂无数据'} />;
};
export default Icon;
